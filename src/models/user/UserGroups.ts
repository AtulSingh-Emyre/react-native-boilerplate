export interface IUserGroupUserData {
  id: string;
  name: string;
  mob: number;
  email: string;
}

export interface IUserGroupModel {
  data: any;
}

export const groupList: any = {default: [], trial: []};
